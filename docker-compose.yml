version: '3.7'
services:
  app:
    container_name: app
    image: app_image
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile.prod
    volumes:
      - ./:/src
    ports:
      - "16000:9000"

networks:
  default:
    external: true
    name: app_main